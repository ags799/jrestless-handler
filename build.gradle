buildscript {
  repositories {
    jcenter()
    maven {
      url 'https://plugins.gradle.org/m2/'
    }
  }
  dependencies {
    classpath 'com.netflix.nebula:nebula-dependency-recommender:4.1.2'
  }
}

plugins {
  id 'nebula.maven-resolved-dependencies' version '5.0.0'
}
